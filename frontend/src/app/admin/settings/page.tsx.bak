"use client"

import { Topbar } from "@/components/topbar"
import { Tabs, TabsContent, TabsList, TabsTrigger } from "@/components/ui/tabs"
import { MpesaSettingsForm } from "./_components/mpesa-settings-form"

export default function SettingsPage() {
  return (
    <div className="flex flex-col min-h-screen bg-zinc-900 text-white">
      <Topbar />
      <div className="flex-1 p-6 space-y-6">
        <div>
          <h1 className="text-2xl font-bold tracking-tight text-transparent bg-clip-text bg-gradient-to-r from-cyan-400 to-blue-500">
            Settings
          </h1>
          <p className="text-sm text-zinc-400">
            Manage your application settings and preferences.
          </p>
        </div>

        <Tabs defaultValue="payment-gateway" className="w-full">
          <TabsList className="bg-zinc-800 border-zinc-700">
            <TabsTrigger value="payment-gateway">Payment Gateway</TabsTrigger>
            <TabsTrigger value="branding">Branding</TabsTrigger>
            <TabsTrigger value="security" disabled>Security</TabsTrigger>
          </TabsList>
          <TabsContent value="payment-gateway" className="mt-4">
            <MpesaSettingsForm />
          </TabsContent>
          <TabsContent value="branding" className="mt-4">
            <div className="bg-zinc-800/50 p-6 rounded-lg border border-zinc-700">
              <p>Branding settings will be here.</p>
            </div>
          </TabsContent>
        </Tabs>
      </div>
    </div>
  )
}